export const DONATION_TIERS = [100, 200, 500, 1000] as const
export const GENERATIONS = Array.from({ length: 45 }, (_, i) => `G${i + 1}`)
export const MIN_CUSTOM_AMOUNT = 10
export const MAX_CUSTOM_AMOUNT = 50000
